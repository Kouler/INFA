Функция перевода из 10-ой в любую СС в виде строки:
```python
#ФУНКЦИЯ ПЕРЕВОДА В N-тую СИСТЕМУ СЧИСЛЕНИЯ

def bitch(n, BASE = 3):

    s = ""

    while n > 0:

        s+= str(n % BASE)

        n//= BASE

    return s[::-1]
```
